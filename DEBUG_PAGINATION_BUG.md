# ページ送りボタン（履歴ナビゲーション）バグのデバッグ記録

## 現象
ユーザー報告：偶に、マウスのページ送りボタン（「次のページ」/「進む」ボタン）を押した時に、過去のページに飛んだり、反応しなかったりする不具合。

## 原因分析
1. `frontend/js/modules/router.js` の `navigateTo(path)` は、常に `window.location.hash = path;` を実行し、ブラウザの履歴スタックに新しいエントリを追加（Push）していました。
2. 学習画面（`study.js`）の「戻る」ボタンを押した際にも `navigateTo('/decks')` が呼ばれていたため、履歴がポップされるのではなく、新たに `/decks` という状態が積まれていました。
3. この結果、履歴スタックに同じページが複数積まれるなどして乱れ、物理的なマウスの「戻る」「進む」ボタンを使用した際の挙動がユーザーの期待値（メンタルモデル）と乖離していました。
   - 「次のページ」が反応しない： UIの「戻る」ボタンによって履歴の最先端にいるため、ブラウザの「進む」履歴が存在しない。
   - 過去のページに飛ぶ： 履歴が積まれすぎているため、「進む」を押しても直前に居た画面に戻らない。

## 修正内容
1. `router.js` に履歴スタックを管理する変数 `_historyCount` を追加しました。
2. `router.js` に履歴を戻るための `goBack()` 関数と、履歴を上書きする `replaceRoute()` 関数を追加しました。
3. `study.js` の「戻る」ボタン（学習中の戻る、完了画面の戻る）のアクションを `navigateTo('/decks')` から `goBack('/decks')` に変更し、正常に `history.back()` が呼ばれるようにしました。
4. `main.js` のフォールバック時の遷移を `navigateTo` から `replaceRoute` に変更しました。

## テスト内容（ユーザー確認待ち）
1. デッキ一覧からデッキを選択し学習画面へ移動する。
2. 画面上の「戻る」ボタンを押してデッキ一覧に戻る。
3. その状態で、マウスの「進む（次のページ）」ボタンを押して、正常に直前の学習画面に戻れるか確認する。
4. 複数回繰り返し、履歴スタックが乱れずに安定して動作するか確認する。
